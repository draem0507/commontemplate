$!
$if{users != null && users.size > 0}
!$
aaa
$!
$if{users != null && users.size > 0}
${user.creationDate # 'yyyy-MM-dd'}
${user.
xxxxx
!$
$if{users != null && users.size > 0}
$!
${user.creationDate # 'yyyy-MM-dd'}
!$
$end